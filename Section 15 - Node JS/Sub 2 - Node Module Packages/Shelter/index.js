const evan = require('./evan')
const jimmy = require('./jimmy')

const allCats = [evan, jimmy]


module.exports = allCats;

// we name it index.js because this file will be the one who get exported when we need to export the dir shelter into modules to other js in outside directory
