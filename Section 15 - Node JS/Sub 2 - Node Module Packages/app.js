const math = require('./math') //refering to script math in this dir
// const {add, square} = require('./math');//when we only need certain things

const allCats = require('./Shelter');

console.log(math);

console.log(math.PI);

console.log('WE GOTS :',allCats);